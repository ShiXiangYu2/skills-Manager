interface:
  display_name: "hello-agent Course Skill"
  short_description: "Source-grounded course Q&A, review, and workflows."
  default_prompt: "Use $hello-agent-mentor to answer questions about hello-agent and cite the course sources."

policy:
  allow_implicit_invocation: true
